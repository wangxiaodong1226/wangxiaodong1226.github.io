---
layout:     post
title:      "用户登陆过程的简要说明"
subtitle:   " \"如何判断用户是否登录？\""
date:       2021-02-02 12:00:00
author:     "Yandong"
header-img: "img/post-bg-2021.jpg"
tags:
    - 计算机网络
---

## 验证是否登录

`用户输入用户名和密码，通过post请求将用户名和密码发送给服务器`
`服务器比对收到的用户名密码和数据库中的数据进行比对，不一致则做出响应，反馈信息给客户端`
`如果比对一致则服务端生成一个session，这个session可以存储在内存、文件、数据库中，同时生成一个与之对应的sessionID作为cookie发送给客户端，`

`比对成功之后反馈信息，这时一般会进行一次重定向，重定向至登录之后的默认页面`。

`判断用户登录则是根据这个sessionID，每次请求会先检查有没有这次类似sessionID的cookie发送过来，没有则认为没有登录，有则验证是否有相应的session，这个session是否过期等，来判断用户是否登录，登录是否过期`。
